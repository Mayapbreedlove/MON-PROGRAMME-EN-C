#include <stdio.h>

void sommesmatrices (int (*matrice1)[2],(*matrice2) [2],(*resultat) [2]") {
for(int i=0,j<1,i++){
        for(int j=0,j<1,j++){
            *(*resultat+i)+j)=*((*matrice1+i)+j) +*((*matrice1+i)+j);
        }
}
    }
